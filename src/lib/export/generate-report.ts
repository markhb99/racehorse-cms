// Phase 5 — @react-pdf/renderer PDF factory.
// Placeholder stub.

import type { Horse, Buyer, HorseStats } from '../types'

export async function generateReport(
  _horse: Horse,
  _buyers: Buyer[],
  _stats: HorseStats,
): Promise<Blob> {
  throw new Error('generateReport not yet implemented — coming in Phase 5')
}
